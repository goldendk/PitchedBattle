
(function(window){
    /**
     * Build boardpiece information for an army so we have something to "play" with.
     *
     * Called from the pb-client-app.js script, which must deploy it.
     * @param color
     * @param side
     * @constructor
     */
   window.buildArmy = function (color, side){


       return { // the army object.



       }

    }



}(window));